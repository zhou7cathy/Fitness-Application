<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SwoleMate | BuffBae</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/b32a05483d.js" crossorigin="anonymous"></script>
    <style> /*Had issues with style.css, putting it here*/
      body {
        background-color: #d6eaff;
        font-family: Arial, sans-serif;
      }
      .header-container {
        background-color: #fff;
        padding: 20px 0;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
      }
      h1 {
        font-size: 3rem;
        font-weight: 700;
        color: #3f3d56;
        margin-bottom: 0;
      }
      .nav-link {
        font-size: 1.2rem;
        color: #3f3d56;
        padding: 10px 0;
        border-bottom: 1px solid rgba(63, 61, 86, 0.1);
        transition: all 0.3s ease;
      }
      .nav-link:hover {
        color: #d6eaff;
        background-color: #3f3d56;
      }
      .nav-link.active {
        color: #d6eaff;
        font-weight: 700;
        border-left: 4px solid #3f3d56;
        background-color: #3f3d56;
      }
      .logout-btn {
        text-align: right;
      }
      main.container {
        padding: 30px 15px;
      }
      section.col-lg-9 {
        margin-bottom: 30px;
      }
      .card {
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        background-color: #fff;
      }
      .card-header {
        background-color: #3f3d56;
        color: #d6eaff;
        font-size: 1.5rem;
        font-weight: 600;
        border-radius: 10px 10px 0 0;
      }
      .card-body {
        color: #3f3d56;
        font-size: 1.2rem;
        line-height: 1.5;
      }
      .btn-primary {
        background-color: #3f3d56;
        border-color: #3f3d56;
        font-weight: 600;
        transition: all 0.3s ease;
      }
      .btn-primary:hover {
        background-color: #1f548d;
        border-color: #d6eaff;
      }
    </style>
  </head>
  <body>
    <div class="header-container">
      <header class="container">
        <div class="row align-items-center">
          <div class="col-lg-9">
            <h1>SwoleMate | BuffBae</h1>
          </div>
          <div class="col-lg-3 logout-btn">
            {{#if logged_in}}
              <button type="submit" class="btn btn-primary" id="logout">Logout</button>
            {{/if}} 
          </div>
        </div>
      </header>
    </div>
    <main class="container">
      <div class="row">
      <nav class="col-lg-3">
            <ul class="nav flex-column">
              <li class="nav-item">
                <a class="nav-link {{#if active_account}}active{{/if}}" href="/account">
                  <i class="fa-solid fa-user fa-lg mr-2"></i> 
                  Account
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link {{#if active_workout}}active{{/if}}" href="/workout">
                  <i class="fa-solid fa-dumbbell fa-lg mr-2"></i> 
                  Workout
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link {{#if active_matrix}}active{{/if}}" href="/matrix">
                  <i class="fa-solid fa-chart-line fa-lg mr-2"></i> 
                  Matrix
                </a>
              </li>
            </ul>
          </nav>
        <section class="col-lg-9"> 
          {{{body}}}
        </section>
      </div>
    </main>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>